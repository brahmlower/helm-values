
# Helm Schema

A helm plugin for generating schema for chart values.

# Features

Generate a values schema:
```
helm schema generate ./charts/my-chart/values.yaml
```

Validate a values schema:
```
helm schema validate ./my-chart.values.yaml
```
